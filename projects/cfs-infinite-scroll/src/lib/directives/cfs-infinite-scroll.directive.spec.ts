import { CfsInfiniteScrollDirective } from './cfs-infinite-scroll.directive';

describe('CfsInfiniteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new CfsInfiniteScrollDirective();
    expect(directive).toBeTruthy();
  });
});
