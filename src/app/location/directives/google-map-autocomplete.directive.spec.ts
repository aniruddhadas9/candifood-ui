import { GoogleMapAutocompleteDirective } from './google-map-autocomplete.directive';

describe('GoogleMapAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleMapAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
